/* Now: Index. */
import { distanceApartListener } from './distance-apart';
import {
  displayLastUpdated,
  displayTimeSince,
  incrementTimeSince,
} from './last-updated';

distanceApartListener();
displayLastUpdated();
displayTimeSince();
incrementTimeSince();
