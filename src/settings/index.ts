/* Settings: Index. */
import { accountDeletionFormListener } from './account-deletion-form';
import { emailFrequencyFormListener } from './email-frequency-form';
import { infoUpdateFormListener } from './info-update-form';

/* eslint-disable jest/require-hook */
accountDeletionFormListener();
emailFrequencyFormListener();
infoUpdateFormListener();
